{
  "name": "express-netlify-app",
  "version": "1.0.0",
  "description": "Express app adapted for Netlify serverless functions",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required for static assets'",
    "simulate:economy": "node scripts/simulate-economy.js",
    "test": "node scripts/self-test.js",
    "generate:aliens": "node scripts/generate-aliens.js 10000"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.23.0",
    "serverless-http": "^3.2.0"
  }
}
